{
  "name": "@sync-engine/plugin-ocr",
  "version": "1.0.0",
  "description": "OCR editing plugin for sync-engine with collaborative features",
  "main": "dist/index.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run build:types && bun run build:bundle",
    "build:types": "tsc --emitDeclarationOnly",
    "build:bundle": "bun build src/index.ts --outdir dist --format esm --target node",
    "test": "vitest --run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts,.tsx",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "ocr",
    "collaboration",
    "sync-engine",
    "plugin",
    "real-time"
  ],
  "author": "Sync Engine Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "uuid": "^9.0.0"
  }
}